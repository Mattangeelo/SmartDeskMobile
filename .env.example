# Copy this file to .env and point it at your backend.
# On a physical device with Expo Go, "localhost" refers to the phone itself,
# so use your computer's LAN IP address (e.g. http://192.168.0.10:3000/api).
EXPO_PUBLIC_API_URL=https://meuapp.local/api
